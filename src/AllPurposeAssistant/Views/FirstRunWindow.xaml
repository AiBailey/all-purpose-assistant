<Window x:Class="AllPurposeAssistant.Views.FirstRunWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="460" Height="330" WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Window.Resources>
        <Style x:Key="ChoiceStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,10,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="Box" Width="18" Height="18" CornerRadius="5" Margin="0,0,9,0"
                                    Background="#99FFFFFF" BorderBrush="#99FFFFFF" BorderThickness="1">
                                <TextBlock x:Name="Mark" Text="✓" Foreground="White" FontSize="13"
                                           HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Box" Property="Background" Value="#806784D8"/>
                                <Setter TargetName="Mark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="92"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#796784D8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontFamily" Value="Microsoft YaHei"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="9">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Background" Value="#33FFFFFF"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#99FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="12">
        <Border CornerRadius="20" ClipToBounds="True" BorderBrush="#99FFFFFF" BorderThickness="1">
            <Border.Effect><DropShadowEffect BlurRadius="26" ShadowDepth="6" Opacity="0.22"/></Border.Effect>
            <Grid>
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#E6DCEBFF" Offset="0"/>
                        <GradientStop Color="#E6EDE4FF" Offset="0.48"/>
                        <GradientStop Color="#E6D9F3EE" Offset="1"/>
                    </LinearGradientBrush>
                </Grid.Background>
                <Image x:Name="BackgroundImage" Stretch="UniformToFill" Opacity="0.1" IsHitTestVisible="False"/>
                <Border Margin="16" Background="#DDF4F3FF" BorderBrush="#99FFFFFF" BorderThickness="1" CornerRadius="14" Padding="26">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid>
                            <TextBlock Text="欢迎使用小帮手" FontSize="22" FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}" FontFamily="Microsoft YaHei"/>
                            <Button Content="&#x00D7;" HorizontalAlignment="Right" VerticalAlignment="Top"
                                    Style="{StaticResource CloseButtonStyle}" ToolTip="关闭" Click="Close_Click"/>
                        </Grid>
                        <TextBlock Grid.Row="1" Margin="0,6,0,0" Text="一个安静、常驻在桌面的本地效率助手。"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontFamily="Microsoft YaHei" FontSize="12"/>
                        <StackPanel Grid.Row="2" Margin="0,14,0,0">
                            <CheckBox x:Name="DesktopShortcutCheckBox" Style="{StaticResource ChoiceStyle}"
                                      Content="在桌面创建快捷方式" IsChecked="True"/>
                            <CheckBox x:Name="LaunchAtLoginCheckBox" Style="{StaticResource ChoiceStyle}"
                                      Content="登录 Windows 时自动启动"/>
                        </StackPanel>
                        <Button Grid.Row="3" Content="开始使用" Style="{StaticResource PrimaryButtonStyle}"
                                HorizontalAlignment="Right" IsDefault="True" Click="Finish_Click"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
