<Window x:Class="AllPurposeAssistant.Views.PinnedScreenshotWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        KeyDown="Window_KeyDown" MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseWheel="Window_MouseWheel">
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="复制图片" Click="CopyImage_Click"/>
            <MenuItem Header="不透明度">
                <MenuItem x:Name="Opacity100MenuItem" Header="100%" Tag="1" IsChecked="True" IsCheckable="True" Click="Opacity_Click"/>
                <MenuItem x:Name="Opacity80MenuItem" Header="80%" Tag="0.8" IsCheckable="True" Click="Opacity_Click"/>
                <MenuItem x:Name="Opacity60MenuItem" Header="60%" Tag="0.6" IsCheckable="True" Click="Opacity_Click"/>
            </MenuItem>
            <Separator/>
            <MenuItem Header="关闭钉图" Click="CloseButton_Click"/>
        </ContextMenu>
    </Window.ContextMenu>
    <Border Background="#FFFFFFFF" BorderBrush="#804A90D9" BorderThickness="1" CornerRadius="6">
        <Grid MouseEnter="Content_MouseEnter" MouseLeave="Content_MouseLeave">
            <Image x:Name="PinnedImage" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
            <Button x:Name="CloseButton" Content="&#x00D7;" Width="28" Height="28"
                    HorizontalAlignment="Right" VerticalAlignment="Top" Margin="6"
                    Background="#CC2C3E50" Foreground="White" BorderThickness="0"
                    FontSize="20" FontWeight="Light" Cursor="Hand"
                    Visibility="Collapsed" Click="CloseButton_Click"/>
        </Grid>
    </Border>
</Window>
